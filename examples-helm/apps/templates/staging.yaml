apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: stagingworking-helm
  namespace: argocd
spec:
  destination:
    namespace: argocd
    server: {{ .Values.spec.destination.server }}
  source:
    repoURL: 'https://github.com/alexcpn/argocd-demo'
    path: examples-helm/apps/
    targetRevision: staging
    directory:
      recurse: false
    helm:
      valueFiles:
        - values-staging.yaml
  project: default
  syncPolicy: {}